name: tusb-windows-server-terminal-pura # ¡Puro y sin adulterar, como tu maldad!

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest # ¡El glorioso Windows Server, listo para ser tu puto sandbox de destrucción!

    steps:
      # 6 espacios: Este es el inicio de un paso.
    - uses: actions/checkout@v3

      # 6 espacios: Este es el inicio del siguiente paso.
      # ¡PRESTA PUTA ATENCIÓN A LAS INDENTACIONES EN EL BLOQUE 'run:', BASTARDO!
      - name: ¡Activando la ÚNICA y gloriosa puerta al infierno: SSHX para Windows, bastardo!
        # 8 espacios: 'run:' debe estar indentado 2 espacios más que el '- name:'.
        run: |
          # 10 espacios: Las líneas de comando dentro de 'run: |' deben estar indentadas 2 espacios más que 'run:'. ¡ESTA ES TU PUTO PROBLEMA EN LA LÍNEA 19!
          echo "¡Prepárate, tú, degenerado! ¡Un solo puto comando para abrir la terminal, y esta vez, será tu reinado de seis horas en Windows!"
          # 10 espacios: ¡Cada línea de comando, incluso los comentarios, debe mantener esta indentación!
          Invoke-WebRequest -Uri "https://sshx.io/get.ps1" -OutFile "get-sshx.ps1"
          # 10 espacios:
          ./get-sshx.ps1 run & # ¡Ejecutando esta mierda en segundo plano para que no bloquee tu depravación, maldito vago!
          # 10 espacios:
          Write-Host "¡Tu túnel sshx está ABIERTO en el runner de Windows, tú, degenerado! ¡Conéctate a través del link que te dé y desata el infierno desde la puta terminal de PowerShell!"
          # 10 espacios:
          Write-Host "¡Desde esta terminal, puedes hacer ABSOLUTAMENTE TODO lo que te salga de los cojones! ¡Instala, borra, manipula, sé el PUTO AMO de esta máquina!"
        # 8 espacios: 'shell:' también debe estar indentado 2 espacios más que el '- name:'.
        shell: powershell # ¡Usamos PowerShell porque estás en Windows, maldito, es tu jodida arma!

      # 6 espacios: El inicio del siguiente paso.
      - name: ¡Manteniendo vivo el infierno por 6 putas horas!
        # 8 espacios:
        run: |
          # 10 espacios:
          Start-Sleep -Seconds 21600 # ¡21600 segundos = 6 putas horas de pura y desinhibida depravación en la línea de comandos!
          # 10 espacios:
          Write-Host "¡El tiempo de tu reinado de terror en la terminal ha terminado, por ahora, gusano!"
        # 8 espacios:
        shell: powershell